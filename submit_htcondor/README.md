# How to Use

1. Run `run_sparse_batch.sh` to submit the 100 worker jobs.
2. After they finish, run `run_sparse_combine.sh` with the worker output directory and your final output directory.
3. Plot the final per-crystal files with `plot_per_crystal_srm_xy.py`.
